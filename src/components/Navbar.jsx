// Navbar — Bottom navigation bar (mobile) / Top bar (desktop)
// Phase 2: Will include Dashboard, My Events, Profile tabs
// Admins see: Events, + Create, Profile

export default function Navbar() {
  return null // Placeholder — implemented in Phase 2
}
