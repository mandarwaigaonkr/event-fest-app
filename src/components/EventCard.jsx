// EventCard — Displays event info with registration status
// Phase 3: Shows name, date, venue, spots remaining, register button

export default function EventCard() {
  return null // Placeholder — implemented in Phase 3
}
