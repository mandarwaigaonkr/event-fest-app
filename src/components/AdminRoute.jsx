// AdminRoute — Restricts access to admin-only pages
// Phase 2: Checks role === 'admin' in real time, redirects otherwise

export default function AdminRoute({ children }) {
  return children // Placeholder — implemented in Phase 2
}
