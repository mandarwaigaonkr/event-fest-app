// useEvents — Custom hook for fetching events from Firestore
// Phase 3: Real-time listener on events collection

export function useEvents() {
  return { events: [], loading: true } // Placeholder — implemented in Phase 3
}
